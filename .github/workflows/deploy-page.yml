name: Build and Deploy

on:
  push:
    branches:
      - main  # This workflow will be triggered when changes are pushed to main branch
  schedule:
    - cron: '0 0 * * *' # Schedule to run daily at midnight

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'  # Updated Node.js version for better compatibility

      - name: Cache dependencies
        uses: actions/cache@v2
        with:
          path: |
            gatsby/node_modules/
            gatsby/.cache/
            public/
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }} # Creates a unique key for the cache based on the OS and the hash of package-lock.json file
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Fetch catalogs
        run: |
          mkdir -p gatsby/catalogs  # Ensure the catalogs directory exists
          node gatsby/fetch-catalogs.js  # Execute the script to fetch catalogs

      - name: Install and Build
        run: |
          cd gatsby
          npm install
          ./node_modules/.bin/gatsby build --prefix-paths

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./gatsby/public

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
